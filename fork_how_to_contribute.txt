HOW to contribute?

fork mo yung repo ko
	git clone (link) mo yung repo mo
	composer install
	cd pet_store
	git branch name
	git checkout branchname
		gawa ka na ng folder at code mo
	git add .
	git commit -m 'comment kung anong ginawa mo'
	git push -u origin branch_name
	punta ka na sa github mo click mo na yung  

git checkout master --punta master


para ma pull ang repo ko  

git remote add upstream https://github.com/lim-crypto/pet_store.git
git fetch upstream 
git rebase upstream/master 

